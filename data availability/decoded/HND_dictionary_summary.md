# Dictionary Summary: HND — EPHPM 2025

**Source files:** DD EPHPM 2025.xlsx  
**Variables decoded:** 356  
**Variables with value codes:** 0  
**Tables/modules:** 2 (Variables, Valores)  

---

## HDMF Variable Coverage Pre-Check

Status is based on keyword matching against variable names and labels.
LIKELY_FOUND means a concept keyword appeared — manual verification still required.

| Domain | HDMF Variable | Status |
|--------|---------------|--------|
| Identifiers & weights | `pais_c` | ✗ NOT_FOUND |
| Identifiers & weights | `idh_ch` | ✗ NOT_FOUND |
| Identifiers & weights | `idp_ci` | ✗ NOT_FOUND |
| Identifiers & weights | `factor_ci` | ✓ LIKELY_FOUND |
| Identifiers & weights | `factor_ch` | ✗ NOT_FOUND |
| Demographics | `edad_ci` | ✓ LIKELY_FOUND |
| Demographics | `sexo_ci` | ✓ LIKELY_FOUND |
| Demographics | `relacion_ci` | ✓ LIKELY_FOUND |
| Demographics | `miembros_ci` | ✓ LIKELY_FOUND |
| Migration | `migrante_ci` | ✗ NOT_FOUND |
| Migration | `mig_pais_ci` | ✗ NOT_FOUND |
| Migration | `migrantiguo5_ci` | ✗ NOT_FOUND |
| Employment status | `condocup_ci` | ✓ LIKELY_FOUND |
| Employment status | `emp_ci` | ✓ LIKELY_FOUND |
| Employment status | `desemp_ci` | ✓ LIKELY_FOUND |
| Employment status | `pea_ci` | ✗ NOT_FOUND |
| Job characteristics | `formal_ci` | ✓ LIKELY_FOUND |
| Job characteristics | `tipocontrato_ci` | ✗ NOT_FOUND |
| Job characteristics | `horaspri_ci` | ✓ LIKELY_FOUND |
| Job characteristics | `horastot_ci` | ✗ NOT_FOUND |
| Job characteristics | `cotizando_ci` | ✓ LIKELY_FOUND |
| Job characteristics | `afiliado_ci` | ✓ LIKELY_FOUND |
| Education | `aedu_ci` | ✓ LIKELY_FOUND |
| Education | `edu_isced` | ✓ LIKELY_FOUND |
| Education | `edu_hdmf` | ✓ LIKELY_FOUND |
| Income | `ylm_ci` | ✓ LIKELY_FOUND |
| Income | `ylnm_ci` | ✗ NOT_FOUND |
| Income | `ynlm_ci` | ✓ LIKELY_FOUND |
| Income | `ytot_ci` | ✓ LIKELY_FOUND |
| Income | `remesas_ci` | ✓ LIKELY_FOUND |
| Income | `remesas_ch` | ✗ NOT_FOUND |

**Coverage: 19/31 HDMF variables likely found (61%)**

---

## Decode Notes

- Source: DD EPHPM 2025.xlsx (2 sheet(s))
- Sheet 'Variables': UNKNOWN layout — best-effort extraction
-   → 342 variables (best-effort — verify manually)
- Sheet 'Valores': UNKNOWN layout — best-effort extraction
-   → 704 variables (best-effort — verify manually)
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '12' — later file takes precedence
-   Duplicate variable '13' — later file takes precedence
-   Duplicate variable '14' — later file takes precedence
-   Duplicate variable '15' — later file takes precedence
-   Duplicate variable '16' — later file takes precedence
-   Duplicate variable '17' — later file takes precedence
-   Duplicate variable '18' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '12' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '12' — later file takes precedence
-   Duplicate variable '13' — later file takes precedence
-   Duplicate variable '14' — later file takes precedence
-   Duplicate variable '15' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '98' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '12' — later file takes precedence
-   Duplicate variable '13' — later file takes precedence
-   Duplicate variable '14' — later file takes precedence
-   Duplicate variable '97' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '97' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '97' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '97' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable 'A' — later file takes precedence
-   Duplicate variable 'B' — later file takes precedence
-   Duplicate variable 'C' — later file takes precedence
-   Duplicate variable 'D' — later file takes precedence
-   Duplicate variable 'E' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '12' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '12' — later file takes precedence
-   Duplicate variable '13' — later file takes precedence
-   Duplicate variable '14' — later file takes precedence
-   Duplicate variable '15' — later file takes precedence
-   Duplicate variable '16' — later file takes precedence
-   Duplicate variable '17' — later file takes precedence
-   Duplicate variable '18' — later file takes precedence
-   Duplicate variable '19' — later file takes precedence
-   Duplicate variable '20' — later file takes precedence
-   Duplicate variable '21' — later file takes precedence
-   Duplicate variable '22' — later file takes precedence
-   Duplicate variable '23' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '23' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '11' — later file takes precedence
-   Duplicate variable '12' — later file takes precedence
-   Duplicate variable '13' — later file takes precedence
-   Duplicate variable '14' — later file takes precedence
-   Duplicate variable '15' — later file takes precedence
-   Duplicate variable '16' — later file takes precedence
-   Duplicate variable '17' — later file takes precedence
-   Duplicate variable '18' — later file takes precedence
-   Duplicate variable '19' — later file takes precedence
-   Duplicate variable '20' — later file takes precedence
-   Duplicate variable '21' — later file takes precedence
-   Duplicate variable '22' — later file takes precedence
-   Duplicate variable '23' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '8' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '10' — later file takes precedence
-   Duplicate variable '23' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '9' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '4' — later file takes precedence
-   Duplicate variable '5' — later file takes precedence
-   Duplicate variable '6' — later file takes precedence
-   Duplicate variable '7' — later file takes precedence
-   Duplicate variable '99' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
-   Duplicate variable '1' — later file takes precedence
-   Duplicate variable '2' — later file takes precedence
-   Duplicate variable '3' — later file takes precedence
